throw new Error(
  "The standalone no-browser helper is disabled because secure E2EE v2 requires the audited browser runtime. "
  + "Open the survey URL and use window.mcpSurveys or the mcp-surveys/browser/v2 meta bridge instead.",
);
